const nomes = ['Maria', 'João', 'Eduardo', 'Gabriel', 'Júlia'];

// array.splice(index, quantidadeDeElementosDeletados, elemento1, elemento2, ...)

// array.pop() = array.splice(array.length, 1)
// array.shift() = array.splice(0, 1)
// array.push() = array.splice(array.length, 0, elemento1, elemento2, ...)
// array.unshift() = array.splice(0, 0, elemento1, elemento2, ...)

// basicamente controla o array com splice