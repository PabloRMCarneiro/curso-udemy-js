// operador ternario ? :

const pontuação = 999;
 
const mensagem = pontuação >= 1000 ? 'Parabéns' : 'Você não ganhou';

console.log(mensagem);

/* if(pontuação >= 1000){
    console.log('Parabéns');
}
else{
    console.log('Você não ganhou');
} */
