const frutas = ['abacaxi', 'banana', 'caju', 'damasco', 'uva'];

for (let i in frutas){
    console.log(frutas[i])
}

const pessoa = {
    nome: 'João',
    sobrenome: 'Pessoa',
    idade: 20
}

for (let i in pessoa){
    console.log(pessoa[i])
}